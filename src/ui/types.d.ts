export type ColorScannerResponse = {
    colors: Array[];
};

export type EventBlast = {
    action: string;
};

export type TColor = {
    red: number;
    green: number;
    blue: number;
    alpha: number;
};
